import Playground from "@/components/Playground";

export const metadata = {
  title: "Playground — GambiarraScript",
  description: "Roda GambiarraScript direto no navegador via WASM.",
};

export default function PlaygroundPage() {
  return <Playground />;
}